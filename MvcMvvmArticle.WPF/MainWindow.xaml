<Window x:Class="MvcMvvmArticle.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MvcMvvmArticle.WPF"
        xmlns:mvvm="clr-namespace:MvcMvvmArticle.MVVM"
        mc:Ignorable="d"
        d:DataContext="{mvvm:ApplicationViewModel}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefintion Width="Auto" />
            <ColumnDefintion Width="*" />
            <ColumnDefintion Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="Name" />
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding NameSearch}" />
        <Button Grid.Column="2" Grid.Row="0">Search</Button>
    </Grid>
</Window>
